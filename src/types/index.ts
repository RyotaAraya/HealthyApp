// データ型で使用
export type Meal = {
    id: number
    url: string
    date: string
}

// Menuなどで使用
export type Hexagon = {
    title: string
    icon: string
}

export type Transition = {
    title: string
    url: string
    description: string
}
